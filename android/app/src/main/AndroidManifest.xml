<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!-- v9.3.0：stateHidden 保留（回前台不自动弹键盘），补 adjustResize——此前只靠 viewport
             interactive-widget=resizes-content，但原生窗口不缩（ADJUST_UNSPECIFIED 在部分内核落到 pan）
             时 CSS 那条完全空转：正文敲 /pet 的确认浮层 fixed bottom:76px 被推出可视区，App 内
             桌宠领养链实际不可达（用户实锤「App 里看不到桌宠」）。adjustResize 后键盘弹出布局视口
             真实压缩，底部浮层（确认条/开奖卡/问候卡）一律贴键盘上沿。 -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- v8.1.0 App Links（§K2 另一半）：点笔记域名链接直达 APK。
                 assetlinks.json 已是真指纹 statement（release 证书 SHA-256）→ 本版部署即校验通过、
                 直达生效——MainActivity 冷/热启 URL 转发（origin 归一防跨源锁屏）同版落地。
                 真机回归：点 note/biji 域链接应开进 App 对应笔记。 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="note.xuyinji.com.cn" />
                <data android:scheme="https" android:host="biji.xuyinji.com.cn" />
            </intent-filter>

        </activity>

        <!-- v8.1.0 应用内打开链接的子 WebView 页（LinkOpenPlugin.openInApp 拉起，不导出） -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".link.LinkViewActivity"
            android:exported="false" />

        <!-- v5.51：通知触发接收器（AlarmManager setAlarmClock 唤醒） -->
        <receiver
            android:name=".rem.RemReceiver"
            android:exported="false" />

        <!-- 开机/覆盖安装/快速开机/时间或时区变更后重排接收器 -->
        <receiver
            android:name=".rem.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- v5.51：本地通知所需 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Android 13+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" /> <!-- Android 12+ -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" /> <!-- Android 14+ 替代 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- v9.3.0：应用内升级——仅覆盖安装用（UpdatePlugin.install 拉系统安装器）。
         下载走 DownloadManager 落应用私有外部目录，不需要任何存储/网络新权限。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- v7.7.0：扫一扫——ML Kit BarcodeScanner 插件自带 manifest 为空，CAMERA 必须 app 声明；
         原生 requestPermissions() 与 WebView 扫码层 getUserMedia 两条路径都依赖它 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>